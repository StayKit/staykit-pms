node_modules
.next
out
build
coverage
*.tsbuildinfo
next-env.d.ts

# Reference prototype — not part of the shipped app
design

# Next.js .well-known routes live in a dot-dir, which ESLint ignores by
# default; re-include so lint-staged can lint files like the OAuth metadata route.
!**/.well-known
!**/.well-known/**
